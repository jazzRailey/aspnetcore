@model OdeToFood.Models.Restaurant

<section>
    <h3>@Model.Name</h3>
    <div>
        Cuisine : @Model.Cuisine
    </div>
    <div>
        <a href="home/details/@Model.Id">Details</a> |
        @Html.ActionLink("GO", "Details", new { id = Model.Id }) |
        <a asp-action="Details" asp-route-id="@Model.Id">More</a>

    </div>
</section>
